import React from 'react'

export const AuthRouter = () => {
    return (
        <div>
            {/* 
            
            Router no va
            switch
            path= /auth/login
            component=login screen
            
            path= /auth/login
            component=login screen

            redirect to = "/auth/login"
            
            */}
        </div>
    )
}
